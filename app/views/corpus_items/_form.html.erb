<%= form_for(@corpus_item) do |f| %>
  <% if @corpus_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@corpus_item.errors.count, "error") %> prohibited this corpus_item from being saved:</h2>

      <ul>
      <% @corpus_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
